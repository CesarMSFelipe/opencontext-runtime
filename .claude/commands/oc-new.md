---
description: Start a new SDD change — runs the full flow automatically
---

Start a new spec-driven change and drive the whole flow in order, switching to each phase's persona and using OpenContext's MCP tools:
explore (Explorer) -> propose/spec/tasks (Orchestrator) -> design (Architect) -> approval gate -> apply (Builder, tests first) -> verify (Reviewer) -> archive. Build context with `opencontext_context` and check `opencontext_impact` before any edit; pause for approval before writing code.

Change: $ARGUMENTS
