# Pinned CI/validation toolchain — the SINGLE source of truth used by both the
# GitHub `test` workflow and the local `make ci` target, so local and CI run the
# exact same tools at the exact same versions. A new ruff/mypy release changing
# format rules or adding checks can no longer break CI out of nowhere; bump these
# deliberately (and re-run `make ci`) instead.
ruff==0.15.20
mypy==2.1.0
pytest==9.1.1
# Surfaces pytest failures as GitHub annotations (readable via the public API),
# so a failure on a runner we can't reproduce locally (e.g. Windows) is
# diagnosable without log access. No-op outside GitHub Actions.
pytest-github-actions-annotate-failures==0.4.2
build==1.5.0
types-PyYAML==6.0.12.20260518
types-requests==2.33.0.20260518
