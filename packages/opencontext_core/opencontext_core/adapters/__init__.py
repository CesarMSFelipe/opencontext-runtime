"""Agent adapters for AI coding tool integration and boundary dispatch.

The adapter classes live in submodules and are imported directly where needed
(``from opencontext_core.adapters.aider import AiderAdapter``,
``from opencontext_core.adapters.boundary import BoundaryService``, etc.). This
package exposes no re-export surface of its own.
"""

from __future__ import annotations
